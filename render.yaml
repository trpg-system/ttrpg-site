services:
  - type: web
    name: ttrpg-site
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ttrpg-db
          property: connectionString

databases:
  - name: ttrpg-db
    plan: free
